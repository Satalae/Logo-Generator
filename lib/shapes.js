//Shape Classes
class triangle {
    constructor(shape, shapeColor) {
        this.shape = shape;
        this.shape = shapeColor;
    }
}

class circle {
    constructor(shape, shapeColor) {
        this.shape = shape;
        this.shape = shapeColor;
    }
} 
class square {
    constructor(shape, shapeColor) {
        this.shape = shape;
        this.shape = shapeColor;
    }
}

//export
module.exports = {
    triangle : triangle,
    circle : circle,
    square : square
};